{
    "version": 2,
    "builds": [
      {
        "src": "./package.json",
        "use": "@vercel/node"
      }
    ],
    "scripts": {
      "start": "node index.js",
      "app": "node index.js",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
}